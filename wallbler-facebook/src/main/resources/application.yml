server:
  port: 8081

spring:
  application:
    name: wallblerfacebook
  datasource:
    url: "jdbc:mariadb://192.168.1.14:3306/data"
    username: "stas"
    password: "141292"
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: "org.hibernate.dialect.MySQL5Dialect"
        format_sql: true
  config:
    import: "optional:configserver:http://localhost:8888"
#  rabbitmq:
#    host: localhost
#    port: 5672
#    username: guest
#    password: guest
#  cloud:
#    function:
#      definition: "uppercase|reverse"
#    stream:
#      bindings:
#        wallblerChannel:
#          destination: wallbler.exchange
#          contentType: application/json
#          group: wallblerGroup

management:
  endpoints:
    web:
      base-path: "/admin"
      exposure:
        include: "*"
  endpoint:
    env:
      post:
        enabled: true

wallbler:
  type: FACEBOOK
